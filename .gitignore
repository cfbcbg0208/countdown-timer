# 검토용 스크래치 (프롬프트·스크린샷) — 버전관리 제외
.prompts/

# docs/ 에 실수로 저장된 스크린샷·날짜형 프롬프트 스크래치도 공개 금지
# (정식 문서 docs/deploy.md 등은 글자로 시작하므로 영향 없음)
docs/*.png
docs/*.jpg
docs/*.jpeg
docs/*.webp
docs/[0-9]*.md

# OS / 에디터 부산물
Thumbs.db
Desktop.ini
.DS_Store
.idea/

# 빌드 산출물 (배포용 dist는 tools/build-dist.mjs 로 재생성)
dist/

# (참고) 런타임 의존성 0 — node_modules 없음
